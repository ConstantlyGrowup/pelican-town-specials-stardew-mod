# Session｜v1.5.7 Release（原料映射默认启用 RAG + main 全量同步）

| 字段 | 值 |
|---|---|
| session_id | `2026-09-24-v1-5-7-release` |
| status | `verification` |
| session_type | `release` |
| owner | WorkBuddy 主会话（单会话直接实施，沿用既有发布范式） |
| started_at | `2026-09-24` |
| user_authorization | `2026-09-24 用户授权把 M14 RAG 改进与 CI 优化同步 main，并以 v1.5.7 安装包发布；默认 RAG 切换经 AskUserQuestion 确认` |
| base_commit | `471153c docs: record M14 CI closeout push` |
| feature_commit_mvp | `d2a57f1 feat: enable rag ingredient mapping by default` |
| release_version | `v1.5.7` |

## 目标与边界

把 M14 的原料映射改进（本地 RAG 检索 + Provider 候选校验，Gold ID 94/120 vs 旧链路 56/120）作为默认链路随 v1.5.7 发布；同时把 M14 期间完成的 CI 分层优化与文档同步到 `main`。不改变试用额度/schema/导出/Canonical 逻辑；RAG 资源不可用时按既有设计 fail-open 回退旧链路。

## Acceptance Ledger

- **V157-001**：`DEFAULT_INGREDIENT_RETRIEVAL_BACKEND` 为 RAG；资源缺失时 fail-open 回退 legacy 且生成不中断。
- **V157-002**：EXE/API/OpenAPI/诊断/installer/workflow 全链路版本一致为 1.5.7。
- **V157-003**：repo、前后端、OpenAPI、静态检查、RAG 模型/索引构建、Windows bundle/installer 门禁全绿；正式 Release 三项资产独立下载校验。
- **V157-004**：main 与 MVP 除各自 README/截图外内容一致（整树同步提交）。
- **V157-005**：构建产物不包含 Key 或用户数据；RAG 模型/索引为离线打包资源。

## 已完成证据

- main 整树同步 `9c7c6ab`（87 文件：M14 Task61–67 代码/评测/文档 + CI 分层 + 修复）；随后发现 `scripts/check_product_copy.py` 的 README 结构锚点按 MVP 开发版 README 写、在 main 用户手册上误报，修复为「锚点检查仅适用于引用 docs/development 的开发版 README」：main `dbe8ab5`、MVP cherry-pick `67e6085`。
- RAG 默认切换 `d2a57f1`：`DEFAULT_INGREDIENT_RETRIEVAL_BACKEND = RAG`；改写两个依赖旧默认的测试（默认断言 + legacy 回退不触发 verifier）；全量 backend `1072 passed / 2 skipped`。
- 版本链 `a968ada`：13 文件 → 1.5.7（含 schema 同步）；repo 门禁 `53 passed`。
- `build_windows.ps1` 全流程通过：backend `1072 passed / 2 skipped`、frontend `231 passed`、ingredient-RAG 模型与索引构建、OpenAPI drift、PyInstaller、EXE 图标/版本身份与 bundle 内容门全绿。
- `build_installer.ps1` 通过：`PelicanTownSpecials-Setup-v1.5.7.exe` 构建成功（SHA256 `C6BEBE28…530617E0F`）。
- push 双分支、tag 与远端 Release 三资产独立核验待执行（本会话无推送凭证，由用户在终端执行）。
