# Session｜v1.5.0 Release

| 字段 | 值 |
|---|---|
| session_id | `2026-08-31-v1-5-0-release` |
| status | `verification_complete`（用户已授权 push/tag/Release） |
| session_type | `release` |
| owner | Codex 主 Agent（M11 全量接管） |
| started_at | `2026-08-31` |
| user_authorization | `2026-08-31 用户明确验收 M11 并授权推送及更新 Release 版本` |
| base_commit | `3e59447 feat: complete milestone 11 trial recovery UX` |
| acceptance_contract_id | `mvp-v1-5-0-release-v1` |
| revise_round | `0` |
| context_packet | `docs/plans/2026-08-31-v1-5-0-release-packet.md`（gitignored） |
| focused_commit | `cd03d2a build: prepare v1.5.0 release chain` |

## 目标与边界

把已验收 Milestone 11 作为下一个 minor 版本 `v1.5.0` 发布。只同步版本身份、工作流默认值、发布包说明、GitHub Release notes 和锁定测试；不改变 M11 产品行为、额度 N、Canonical 阈值、遥测合同或 Secret/Variable 注入方式。

## 冻结 Acceptance Ledger

- **V150-001**：EXE FileVersion/ProductVersion、API/OpenAPI、诊断版本、installer 默认值与 `.iss` 全部为 `1.5.0`。
- **V150-002**：build/release workflow 默认版本、示例、tag 解析和 release 资产命名一致为 `1.5.0` / `v1.5.0`；既有最小权限、trial Secret 与 telemetry Variables 转发不变。
- **V150-003**：bundle README 和 GitHub Release notes 明确 v1.5.0/M11 主要变化，并准确说明可先试用或配置个人服务。
- **V150-004**：OpenAPI 重新生成且无 drift；版本锁定测试同步，repo tests、前后端全量门禁、PyInstaller、bundle smoke、installer build/smoke 全绿。
- **V150-005**：不提交 Key、遥测生成资源、构建产物或无关未跟踪文件；不改产品逻辑。

## 当前进度

- M11 focused commit `3e59447` 已创建；等待 `luna_worker` 实施 v1.5.0 版本链。
- `luna_worker` 已在冻结文件范围内完成 1.5.0/v1.5.0 同步、OpenAPI 生成和 M11 发布说明，`implementation_scope_delta: none`，未读取 Key、未 commit/push。
- 主 Agent focused 复跑：repo version/release tests `11 passed`，`check_release_version 1.5.0`、diff-check 全绿。等待 detector 独立审阅；完整 Windows/installer 门禁由主 Agent在 PASS 后执行。
- detector 返回 `PASS`：V150-001..005 全部满足，`must_fix=[]`、`optional_hardening=[]`、`scope_delta=none`。下一步执行带已配置 Release telemetry 资源与 gitignored trial 资源的完整 Windows、bundle、installer 门禁。
- 首次完整 `build_windows.ps1` 已通过 backend `889 passed / 2 skipped`、frontend `191 passed` 和生产构建，在 OpenAPI drift gate 停止；生成物只有预期的 `1.4.0 → 1.5.0` 差异。该 gate 要求版本链先进入 Git 基线，因此先创建 detector PASS 的版本 focused commit，再从提交后工作树重跑完整门禁。
- 版本链 focused commit `cd03d2a` 已创建；从该 Git 基线重跑配置完整的 Release 构建全部通过：backend `889 passed / 2 skipped`、frontend `191 passed`、生产构建、OpenAPI drift、ignore policy、telemetry manifest、PyInstaller、内容门、EXE 图标与 File/ProductVersion `1.5.0`。
- Bundle smoke PASS：递归 SQLite、health、静态主页、非空 Registry 与同一临时工作区二次启动均正常。
- Inno Setup 6.7.3 编译 `PelicanTownSpecials-Setup-v1.5.0.exe` 成功；隔离 install → health → 二次启动 → reinstall → uninstall 全流程 PASS，工作区 marker 保留。
- 本地 RC 资产：setup `47,028,710` B，SHA256 `B13E3D11336C701C7D0AA722CF5CCF719068B8A6CD735782BD0B5C127D99713C`；portable ZIP `53,414,275` B，SHA256 `25FFC76E4FE3F07C56A2F839928518E6B9E68A045263EB40ED9F6395DE6F6142`。
- Bundle 试用资源存在、长度 51 且与 gitignored 本地资源 SHA256 匹配；Release telemetry 资源 schema/HTTPS host/project token/enable gate 完整。未读取或输出任何 Key/token 值，资源和构建产物保持 Git ignored。
- 下一步按用户授权推送 `feat/mvp-implementation`，创建 annotated tag `v1.5.0` 并推送，等待 GitHub Actions Release workflow 后核验远端三项资产与 SHA256SUMS。
