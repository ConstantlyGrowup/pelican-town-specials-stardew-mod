# Session｜M14 原料 RAG 人审简版报告

| 字段 | 值 |
|---|---|
| session_id | `2026-09-24-m14-rag-human-review-report` |
| status | `accepted` |
| session_type | `documentation_maintenance` |
| owner | Codex 主 Agent |

## 目标与范围

按用户要求从已冻结的 24 菜及 96 菜对照和 JEV 事后翻译歧义裁决中，另出一份人审可读的简版报告，只保留全菜正确、单原料合理、召回／最终命中、非兜底覆盖与耗时。用户后续明确：只要菜名不重复即可合并，原料 query 重复属于不同菜复用食材的正常情况。只读复算，不重新运行映射或调用 JEV，不修改原始评测、产品代码或默认链路。

## 核验与结果

- 两批中英文菜名均不重复，原料 query 有 30 个重叠（旧集 50 个不同 query）；按用户最新确认以不同菜为单位合并，共 120 菜／360 原料项。
- 从 ignored `output/m14-task66-1/metrics.json` 逐项复算：翻译修正命中旧 12／新 15 项；全菜正确旧 `48/96`、新 `71/96`；单原料合理旧 `218/288`、新 `260/288`；非兜底覆盖 `243/288`、`288/288`；Top 5 至少一个 Gold `234/288`、`274/288`；最终非兜底且命中 Gold `204/288`、`252/288`。耗时引用已完成的 Task66.1 冻结报告。
- 旧 24 菜另从 Task66 同集指标逐项复算：两侧 `Wheat Flour`→`246` 各 3 项翻译歧义修正，全菜旧 `17/24`、新 `21/24`；单项旧 `64/72`、新 `68/72`；Top 5 命中 `70/72`、`72/72`；最终非兜底且 Gold 命中 `60/72`、`66/72`；非兜底 `71/72`、`72/72`。
- 合并结果：全菜 `65/120`→`92/120`，单项 `282/360`→`328/360`，Top 5 `304/360`→`346/360`，最终非兜底且 Gold 命中 `264/360`→`318/360`，非兜底 `314/360`→`360/360`。原始逐项耗时两批合并并按 nearest-rank 重算 p50/p95：旧 `1.089/1.589 ms`、RAG `6.403/8.833 ms`，各 360 项。
- 人审报告：`docs/development/M14_INGREDIENT_RAG_HUMAN_REVIEW.md`。原 Task64/66/66.1 报告与 raw JSONL 不改；两批分层结果和原始 JEV 结果仍可分别查阅。

## 验收边界

用户已明确接受报告并授权仅本地提交，随后启动新 Task 处理 RAG 对无对应物强行匹配及优化后实测。此 Session 只创建一个 focused 文档提交；未获新的 push、默认切换、正式包或 Release 授权。
