# Session｜v1.5.4 Release（收集品批量删除）

| 字段 | 值 |
|---|---|
| session_id | `2026-09-03-v1-5-4-release` |
| status | `verification` |
| session_type | `release` |
| owner | WorkBuddy 主会话（单会话直接实施，沿用既有发布范式） |
| started_at | `2026-09-03` |
| user_authorization | `2026-09-03 用户验收批量删除功能并授权提交推送；同日确认需要进入正式发布包` |
| base_commit | `6315591 feat(frontend): batch delete selected cookbook dishes` |
| feature_commit_mvp | `6315591 feat(frontend): batch delete selected cookbook dishes` |
| feature_commit_main | `112476f feat(frontend): batch delete selected cookbook dishes` |
| release_version | `v1.5.4` |

## 目标与边界

把已验收的收集品批量删除功能作为 patch 版本 `v1.5.4` 发布。同步版本身份、工作流默认值、发布包说明、Release notes、OpenAPI info.version 与锁定测试；不改变删除端点合同、级联清理逻辑、试用迁移、Canonical、遥测或两分支各自的根 README 定位。

## Acceptance Ledger

- **V154-001**：EXE/API/OpenAPI/诊断/installer/workflow 全链路版本一致为 1.5.4。
- **V154-002**：包内 README 与 GitHub Release notes 说明收集品多选后可批量删除、与打包菜单并列、删除前二次确认。
- **V154-003**：批量删除复用单菜 tombstone DELETE 端点，级联清理与幂等合同不变；无 API schema 变化。
- **V154-004**：repo、前后端、OpenAPI、静态检查、Windows bundle/installer 门禁全绿；正式 Release 三项资产独立下载校验。
- **V154-005**：不提交构建产物、密钥或无关未跟踪文件。

## 当前进度

- 功能已在 MVP `6315591` / main `112476f` 提交并推送（用户在终端完成 push）。
- 版本链 `5441c61`：13 个文件同步 1.5.4；repo 门禁 `52 passed`；OpenAPI 重新导出为 `1.5.4`。
- `build_windows.ps1` 全流程通过：backend `895 passed`、frontend `200 passed`，TypeScript/Vite、OpenAPI drift、ignore policy、telemetry contract、PyInstaller、EXE 图标/版本身份与 bundle 内容门全绿。
- `build_installer.ps1` 通过：`PelicanTownSpecials-Setup-v1.5.4.exe` 构建成功（SHA256 `E3EE00BB…1AA8B05`）。
- push 双分支、tag 与远端 Release 三资产独立核验待执行（本会话无推送凭证，由用户在终端执行）。
