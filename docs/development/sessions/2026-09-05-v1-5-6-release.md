# Session｜v1.5.6 Release（收集品按最新收录优先排序）

| 字段 | 值 |
|---|---|
| session_id | `2026-09-05-v1-5-6-release` |
| status | `verification` |
| session_type | `release` |
| owner | WorkBuddy 主会话（单会话直接实施，沿用既有发布范式） |
| started_at | `2026-09-05` |
| user_authorization | `2026-09-05 用户要求收集品按最新收录优先排列，并同步 main、构建发布到下一安装包版本` |
| base_commit | `d133c43 docs: close v1.5.5 trial migration release` |
| feature_commit_mvp | `a8b70b0 feat: order cookbook by newest acceptance first` |
| release_version | `v1.5.6` |

## 目标与边界

收集品列表改为“最新收录优先”——刚接受进收集品的菜品排在前面。同步版本身份、工作流默认值、发布包说明、Release notes、OpenAPI info.version 与锁定测试。不改变归档记录、schema、删除/打包/分页逻辑、试用迁移或两分支各自的根 README 定位。

## Acceptance Ledger

- **V156-001**：`GET /api/v1/cookbook` 按 `archived_at` 倒序返回，最新收录在前。
- **V156-002**：EXE/API/OpenAPI/诊断/installer/workflow 全链路版本一致为 1.5.6。
- **V156-003**：repo、前后端、OpenAPI、静态检查、Windows bundle/installer 门禁全绿；正式 Release 三项资产独立下载校验。
- **V156-004**：分页、批量删除、打包选择与中英文界面不受影响；无 API schema 形状变化。

## 已完成证据

- 排序实现于 `CookbookService.list()`（`archives.sort(key=archived_at, reverse=True)`）；TDD：新增 `test_cookbook_list_orders_newest_archive_first`（三次归档后断言倒序 + 时间倒序）。
- focused cookbook 应用/API 测试 `14 passed`；全量 backend `950 passed / 2 skipped`；frontend `231 passed`。
- 版本链 `7b04cf2`：13 文件 → 1.5.6（含 schema.d.ts 同步）；repo 门禁 `52 passed`。
- `build_windows.ps1` 全流程通过：backend `950 passed / 2 skipped`、frontend `231 passed`、OpenAPI drift、PyInstaller、EXE 图标/版本身份与 bundle 内容门全绿。
- `build_installer.ps1` 通过：`PelicanTownSpecials-Setup-v1.5.6.exe` 构建成功（SHA256 `7E9A77CD…C9DA130B`）。
- push 双分支、tag 与远端 Release 三资产独立核验待执行（本会话无推送凭证，由用户在终端执行）。
