# Session｜v1.5.3 Release

| 字段 | 值 |
|---|---|
| session_id | `2026-09-02-v1-5-3-release` |
| status | `released` |
| session_type | `release` |
| owner | Codex 主 Agent（`luna_worker` 版本链，`detector` 独立审阅） |
| started_at | `2026-09-02` |
| user_authorization | `2026-09-02 用户明确要求 Task 47 同步 main/MVP、推送并更新最新 Release` |
| base_commit | `3add397 fix: refresh public trial access` |
| acceptance_contract_id | `mvp-v1-5-3-release-v1` |
| revise_round | `0` |
| context_packet | `docs/plans/2026-09-02-v1-5-3-release-packet.md`（gitignored） |

## 目标与边界

把已通过独立审阅的 Task 47 作为 patch 版本 `v1.5.3` 发布。同步版本身份、工作流默认值、发布包说明、Release notes、OpenAPI info.version 与锁定测试；不再改变试用迁移、Provider、API schema、Key、遥测或 Canonical 逻辑。

## Acceptance Ledger

- **V153-001**：EXE/API/OpenAPI/诊断/installer/workflow 全链路版本一致为 1.5.3。
- **V153-002**：包内 README 与 GitHub Release notes 说明公共试用改用 `gpt-image-2`、额度为 5、旧用户升级后一次性恢复完整 5 次；个人服务设置不受影响。
- **V153-003**：Secret/Variables 注入、最小权限、资产命名与 setup/ZIP/SHA256SUMS 合同不变。
- **V153-004**：repo、前后端、OpenAPI、静态检查、Windows bundle/installer 与 smoke 全绿；正式 Release 三项资产独立下载校验。
- **V153-005**：不提交 Key、遥测生成资源、构建产物或无关未跟踪文件。

## 当前进度

- `luna_worker` 已按冻结范围完成 13 个版本链文件，`implementation_scope_delta: none`。
- worker red/green：版本锁定测试先得到 `3 failed / 8 passed`，实现后 `11 passed`；OpenAPI 已重新导出为 `1.5.3`，版本漂移检查与 `git diff --check` 通过。
- 主 Agent 已复跑 focused repo tests：`11 passed`；`check_release_version.ps1 -Version 1.5.3` 与 `git diff --check` 通过。
- `detector` 已按 `mvp-v1-5-3-release-v1` 独立只读审阅并返回 `PASS`（revise round 0，must-fix 为空，scope delta 为 none）。
- 完整 `build_windows.ps1` 通过：backend `893 passed, 2 skipped`、frontend `197 passed`，TypeScript/Vite、OpenAPI drift、ignore policy、telemetry contract、PyInstaller、EXE 图标/版本身份和 bundle 内容门全绿。
- bundle smoke 通过：两次干净启动、health、静态主页、SQLite 扩展与持久化 registry 均正常。
- v1.5.3 installer 构建与隔离 smoke 通过：安装、health、重装、卸载全绿，隔离用户工作区保留。
- 本地 RC：setup `47,026,039` B，SHA256 `01d4e25e95db9a166e89dc484aec35518a7eccab7dbb2a5c8bad09d62a6b5188`；portable ZIP `53,418,573` B，SHA256 `4e5a5b5c4c6c881353e61d7ed7d3137ddc4b8c0e2e051ac73fee95345aa250ed`。
- 两分支同步完成：MVP `feadade`（RC 证据），main `3abcb3a`（版本链 cherry-pick，STATUS 冲突以 MVP 权威版解析）+ `f06a4c4`（控制面补齐），排除用户 README/截图后全树 diff 为空。
- 用户授权后 push 完成：MVP/main 远端与本地一致，annotated tag `v1.5.3` 指向 `feadade`。
- 远端核验：release run `33638152719` success（12m36s）；正式 Release 非 draft/pre-release；setup.exe（43,505,781 B，SHA256 `19d9be48…4ac58c`）、portable ZIP（48,537,548 B，SHA256 `5e9a29b7…aa4d6c6`）与 SHA256SUMS 独立下载，SUMS 与两资产摘要逐项一致。

## Worker Handoff

- result: `READY_FOR_CODEX_REVIEW`
- changed_files: Packet 允许的 13 个发布链文件
- interfaces_changed: 仅 OpenAPI `info.version` 更新为 `1.5.3`，无 endpoint/schema 变化
- secrets/provider/build_artifacts: 未读取 Key、未调用 Provider、未产生产品构建物
- suggested_commit: `release: prepare v1.5.3 trial hotfix`

## Detector Review

- decision: `PASS`
- model: `gpt-5.6-sol / medium`
- checked: 全链路版本、OpenAPI 唯一语义变化、发布说明、workflow Secrets/Variables/权限、focused tests 与范围
- independent tests: `11 passed`；版本漂移检查和 `git diff --check` 通过
- deferred_to_parent: Windows bundle/installer 与 smoke、push/tag、正式 Release 资产独立校验
