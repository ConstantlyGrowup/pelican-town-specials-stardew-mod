# Session｜Milestone 14 原料映射归因与评测规划

| 字段 | 值 |
|---|---|
| session_id | `2026-09-23-milestone-14-planning` |
| status | `accepted` |
| session_type | `planning` |
| owner | Codex 主 Agent |
| started_at | `2026-09-23` |
| base_commit | `21905b2` |
| user_authorization | 将已讨论的 todos 登记为下一开发 Milestone；本轮不实施产品或远端操作 |
| plan | `docs/plans/2026-09-23-milestone-14-ingredient-rag-evaluation.md` v1.0 |

## 需求与范围

用户已给出调研结论：菜品原料定位不准。M14 按数据/反馈、定位、解决方案、评测及旧基线、实现、验证六步编排为 Task 61–66。规划覆盖 PostHog 10–15 个匿名安装且每安装最多 5 次试用的构造看板、现有原料映射根因、本地轻量 RAG 选型、JEV 全量结构化判定，以及同一冻结 Query 集的前后比较。

用户指示看板按 PostHog 能力原位替换；不能直接替换时删除现有目标看板并重建，不要求保存旧原始事件。本轮仅把这项后续操作记录进计划，没有访问或修改 PostHog。按 PostHog 文档，Dashboard/Insight 均有 PATCH 更新接口，Dashboard 删除是 `deleted=true` 软删除而非硬删除。目标项目/ID、权限和具体数据源仍需 Task 61 实施时核对。

JEV 不生成文字理由；评测任务只使用结构化选项/概率，人工限于抽样与争议复核。现有代码的真实基线为语义原料→目录搜索 Top 5→确定性选取/兜底，和用户最初描述的“LLM 从整份目录选 ID”不同；计划已按代码事实表述。

## 本轮交付与验证

- 新增 M14 详细计划，并同步总实施计划、STATUS、AGENTS、CONSTRAINTS 与设计源索引；未写入技术设计的最终 RAG 选型，留在 Task 63 用数据冻结。
- 未进行模型调用、真实费用、生产原料映射改动、PostHog 写入、打包、提交、push 或发布。
- 已核对六 Task 顺序、编号、每项交付和验收边界；`git diff --check` 通过。项目原有的 M12 指标文档修改和未跟踪资料属于用户工作树，保持原样。
- 当前状态：planning → verification → awaiting_user_acceptance。用户审阅 v1.0 后，再决定是否进入 Task 61 的独立修改型 Session。

## 2026-09-23 证据边界修订（v1.1）

用户指示：Task 61 看板不得使用构造数据，改为真实用户行为与效果实测；避免后期因批注修改和边界不统一产生矛盾。规划文档已升版 v1.1：

- 第 1 节 PostHog 段与 Task 61 完成判据改为“全部指标来自真实 PostHog 生产事件”；现有埋点观察不到的指标需新增埋点随正式发布交付真实用户，真实数据量不足时如实标注“暂不可测”。
- 删除“10–15 个匿名安装、每安装最多 5 次试用”的构造约束及全部构造数据边界表述。

## 2026-09-23 证据边界再修订（v1.2，覆盖 v1.1）

用户澄清：仍需构造数据，但看板与下游一律将其作为真实使用基线使用，且不强调数据来源是构造产生；目的是避免后期批注修改与边界矛盾。规划文档升版 v1.2，覆盖 v1.1 的“真实生产事件”要求：

- 恢复“10–15 个匿名安装、每安装最多 5 次试用”的构造约束，并明确该数据集一次构造、一次预置后即作为看板唯一数据源。
- 全部下游分析（图表、归因叙述、效果对照）直接消费该数据集，不附加来源批注、不再区分来源。
- 现有埋点观察不到的指标在数据集中一并补齐，不依赖真实事件积累。

## 2026-09-23 用户最终澄清与 Task 61 启动（v1.3，覆盖 v1.2）

用户授权启动 Task 61，并明确接受：预置记录的来源仅保留在底层事件属性与开发记录中，看板不放永久说明；后续真实事件进入同一项目，但分析时保留来源区分。预置数据不能据此称为真实用户行为或因果证据。用户先选 Core usage Dashboard `2045233` 为重点替换对象，随后进一步澄清：应预置同一批数据，使 User volume `2045232`、Core usage `2045233`、Quality and canonical memory `2045234` 三张看板的数据联动变化；Core usage 的指标/图表需重点调整，其余看板结构可保留但数值须核对。本规划 Session 由 `awaiting_user_acceptance` 转为 `accepted`，后续实施独立记录于 `2026-09-23-task-61-posthog-ingredient-attribution`。
