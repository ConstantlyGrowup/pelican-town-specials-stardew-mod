# Session｜v1.5.5 Release（试用服务迁移 + M13 分页）

| 字段 | 值 |
|---|---|
| session_id | `2026-09-03-v1-5-5-release` |
| status | `released` |
| session_type | `release` |
| owner | WorkBuddy 主会话（单会话直接实施，沿用既有发布范式） |
| started_at | `2026-09-05` |
| user_authorization | `2026-09-05 用户确认本地 trial key 已轮换，要求把 MVP 开发进度同步 main 并走新版本链发布` |
| base_commit | `c4c5cd9 docs: record milestone 13 push` |
| feature_commit_mvp | `8ea63c1 feat: migrate trial provider to totokens` |
| release_version | `v1.5.5` |

## 目标与边界

旧试用站点（yibuapi）额度耗尽，公共试用整体迁移到带额度管控的新站点；同时把已验收的 M13 分页工作（收集品 8 条/页、统一像素分页面板）一并带入 patch 发布。同步版本身份、工作流默认值、发布包说明、Release notes、OpenAPI info.version 与锁定测试。不改变试用额度（5 次）、schema v3 迁移、脱敏合同、个人服务逻辑、API schema 形状或两分支各自的根 README 定位。

## Acceptance Ledger

- **V155-001**：EXE/API/OpenAPI/诊断/installer/workflow 全链路版本一致为 1.5.5。
- **V155-002**：试用预设指向 `https://totokens.cc/v1`，视觉/文本模型 `gpt-5.6-terra`，图像模型 `gpt-image-2`，超时 120/300，最大自动重试 2；设置页默认 Base URL/模型同步迁移。
- **V155-003**：试用脱敏合同不变（错误 details 恒为空，URL/模型/Key 不出客户端）；API schema 形状无变化。
- **V155-004**：repo、前后端、OpenAPI、静态检查、Windows bundle/installer 门禁全绿；正式 Release 三项资产独立下载校验。
- **V155-005**：Key 只存在于 gitignored 本地资源与 GitHub Secret，不进 Git/日志/文档。

## 已完成证据

- 试用迁移 `8ea63c1`（trial.py 预设 + settings.py 默认 + 测试断言/脱敏样本同步）；focused `58 passed`；全量 backend `949 passed / 2 skipped`。
- M13 分页（收集品 8 条/页 + 统一分页像素面板）已在 `d8fb1ed` / `89a8a09` 提交；frontend 全量 `231 passed`。
- 版本链 `f7a272f` + schema 再生成 `6a77cf3`：repo 门禁 `52 passed`。
- `build_windows.ps1` 全流程通过：backend `949 passed / 2 skipped`、frontend `231 passed`、OpenAPI drift、ignore policy、telemetry contract、PyInstaller、EXE 图标/版本身份与 bundle 内容门全绿。
- `build_installer.ps1` 通过：`PelicanTownSpecials-Setup-v1.5.5.exe` 构建成功（SHA256 `84F25B45…F469B01`）。
- GitHub Secret `PTS_TRIAL_API_KEY` 与本地 gitignored `resources/trial/trial_api_key.txt` 均已由用户轮换为新站 Key（Key 不进会话）。
- 首次 release run `33951663016` failure：Task21 e2e 夹具路由未覆盖 Task 57 分页查询串，请求穿透到 Vite 代理导致列表为空；修复 `ee2e885`（路由通配查询串 + 响应补 page/totalPages），本地 e2e `39 passed`。
- 用户删除并重打 tag `v1.5.5`（→ `ee2e885`）后 run `33952701626` success（约 12 分钟）；正式 Release 非 draft/pre-release；setup.exe（43,540,405 B，SHA256 `17708772…5db82233`）、portable ZIP（48,575,512 B，SHA256 `f0bf1e00…7a85e50c`）与 SHA256SUMS 独立下载，SUMS 与两资产摘要逐项一致。
