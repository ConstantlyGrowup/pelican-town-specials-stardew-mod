# 环境变量参考（全部可选）— Pelican Town Specials / 鹈鹕镇新菜单

# 复制本文件为 .env 前请先阅读说明。应用默认从
# %LOCALAPPDATA%\PelicanTownSpecials\workspace 读取工作区，不需要 .env 也能运行。
# 本文件只含空值与说明，不包含任何真实 token。

# 覆盖工作区路径（默认 %LOCALAPPDATA%\PelicanTownSpecials\workspace）
# PTS_WORKSPACE_PATH=

# Ask Gus 最低置信度（0.0–1.0，默认 0.5）
# PTS_ASK_GUS_MIN_CONFIDENCE=

# Provider API Key。请勿把真实 Key 放在 .env 或任何提交的文件中：
# 应用「设置」页会把它写入当前 Windows 用户环境变量 PTS_OPENAI_API_KEY。
# PTS_OPENAI_API_KEY=
